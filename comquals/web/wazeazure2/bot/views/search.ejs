<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Search Results</title>
  <style>body{background:#1a1a1a;color:#e0e0e0;}</style>
</head>
<body>
  <h2>Search Results</h2>
  <div id="results"></div>

  <script nonce="<%= nonce %>">
    const b64 = `<%- contentB64 %>`;

    const str1 = atob(b64);
    let str2 = '';
    for (let i = 0; i < str1.length; i++) {
      str2 += String.fromCharCode(str1.charCodeAt(i));
    }
    document.write(str2);
  </script>
</body>
</html>